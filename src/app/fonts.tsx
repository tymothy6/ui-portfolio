import { Inter, Noto_Serif } from "next/font/google"

export const inter = Inter({ subsets: ['latin'], display: 'swap', })

export const noto_serif = Noto_Serif({ subsets: ['latin'], display: 'swap', variable: '--font-serif', })